import { ProductsOfShelfPipe } from './products-of-shelf.pipe';

describe('ProductsOfShelfPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductsOfShelfPipe();
    expect(pipe).toBeTruthy();
  });
});
